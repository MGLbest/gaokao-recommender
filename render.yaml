services:
  - type: web
    name: gaokao-recommender
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend.app_v3:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
